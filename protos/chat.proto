syntax = "proto3";

package chat;

service ChatService {
  // Bidirectional streaming RPC where client and server can send/receive messages at the same time
  rpc Chat (stream ChatMessage) returns (stream ChatMessage);
}

message ChatMessage {
  string user = 1;
  string message = 2;
}